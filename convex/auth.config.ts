// https://humane-panda-49.clerk.accounts.dev
const authConfig = {
    providers: [
        {
            domain: "humane-panda-49.clerk.accounts.dev",
            applicationID: "convex",
        }
    ]
};

export default authConfig;